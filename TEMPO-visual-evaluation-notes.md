# TEMPO — Ghi chú đánh giá visual concept

## Phạm vi sử dụng

Mười hai ảnh trong bộ `tempo-visual-*` là **visual concept AI để đánh giá bối cảnh, ánh sáng và ngôn ngữ chiến dịch Night Confident**. Chúng không phải ảnh sản phẩm thực tế và không được dùng làm nguồn duy nhất để chốt kích thước, typography nhãn, hoặc logo trước khi có mẫu sản xuất.

## Quan sát đã kiểm tra

| Visual | Quan sát | Quyết định dùng |
| --- | --- | --- |
| `01-pocket-3ml` | Bối cảnh áo khoác, giấy ivory, graphite và teal nhất quán với Night Confident; logo trên chai là bản AI diễn giải, không thay thế logo V2JOY gốc. | Phù hợp đánh giá mood/lifestyle, không dùng để duyệt logo/nhãn. |
| `03-scale-pair` | Phân biệt 3ml/5ml và đặt cạnh điện thoại rõ ràng; tỷ lệ chai do AI dựng vẫn có thể lớn hơn quy cách 3ml thực tế. | Chỉ dùng so sánh concept; tham chiếu kích thước chính thức vẫn là mockup bao bì đã duyệt có iPhone. |

## Nguyên tắc trên landing

Landing công khai hiện dùng các mockup đã duyệt (`primary`, `unboxing`, `scale`) để ổn định nhận diện và tỷ lệ. Bộ 12 visual concept được giữ để người dùng lựa chọn hướng chụp trước sản xuất, không thay thế mockup chính trong các CTA hàng chờ.
