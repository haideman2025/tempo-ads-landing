# QA Playback Video TEMPO — 18/08/2026

| Hạng mục | Kết quả quan sát | Kết luận thao tác |
|---|---|---|
| Chuỗi asset trong mã | `1-3 → 2-2 → 3-2 → 4-2 → 5-2` | Đúng thứ tự năm chapter; Video 05 dùng `motionFinal`, không còn `motionHero`. |
| Phân phối HTTP | Cả năm URL trả `200`, `Content-Type: video/mp4`, `Accept-Ranges: bytes` sau chuyển hướng lưu trữ | Không có lỗi MIME, range hoặc URL. |
| Codec | H.264 High, yuv420p, 1280×720, 24fps cho cả năm tệp | Tương thích với Chrome hiện đại. |
| Landing trong trình duyệt kết nối | Trình duyệt báo fallback cho Video 01 trong một lần kiểm tra; không có stack trace mới trong log console sandbox | Đã thay logic chapter 02–05: ngưỡng quan sát 5%, autoplay muted/inline, ba lần thử lại tăng dần và chỉ fallback khi hết retry hoặc media error thực. |
| Kiểm tra file trực tiếp trên trình duyệt người dùng | Lần điều hướng trực tiếp Video 02 bị timeout từ extension, không phải phản hồi HTTP của tệp | Cần nghiệm thu lại trên `www.v2joy.life` sau khi phiên bản mới được phát hành. |

**Lưu ý:** Bản ghi không tạo đăng ký thử hay làm thay đổi quota 1.000 chai. Nghiệm thu Pixel và Telegram vẫn cần một form hợp lệ do chủ dự án tự gửi.
