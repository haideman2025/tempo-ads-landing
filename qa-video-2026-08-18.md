# QA video nền TEMPO — 2026-08-18

## Phạm vi kiểm tra

Đã thay năm tệp MP4 người dùng cung cấp theo thứ tự `1-3 → 2-2 → 3-2 → 4-2 → 5-2` tại năm chapter 01–05. Tất cả tệp là H.264 1280×720, dài 8–10 giây, và phản hồi HTTP 206 sau redirect khi tải từ môi trường landing.

## Kết quả visual

Ảnh chụp full-page ở desktop 1280×720 và mobile 375×812 cho thấy bố cục không bị vỡ; thứ bậc CTA, form hàng chờ, khối thông tin và chuyển đổi nền graphite/ivory vẫn giữ được. Các video tiếp tục dùng poster ảnh thay thế, `muted`, `playsInline`, vòng lặp, dừng khi rời vùng nhìn và tôn trọng reduced-motion.

## Lưu ý thiết kế

Hệ tín hiệu teal và bố cục editorial hiện đã nhất quán. Không thay đổi copy, quota, form hay logic Pixel trong lần cập nhật video này.

## Kiểm tra sau restart

Procedure `waitlist.status` hiện phản hồi HTTP 200 với `claimed: 0`, `remaining: 1000` và `capacity: 1000`; lỗi truy vấn quota cũ xuất hiện trong log trước migration không còn tái diễn. Ảnh chụp lại desktop và mobile sau restart xác nhận hero/video, CTA và form tải bình thường; không thấy lỗi tràn ngang hoặc chữ che vùng thao tác.
